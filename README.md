# replay-mode-player-component

This repository contains a standalone extraction of `ReplayModePlayer.vue` from the project.

## Structure

- `src/components/views/ReplayModePlayer.vue`

## Notes

- This is a direct component extraction.
- It depends on the original project environment (Vue app, composables, libs, assets).
- If you want this repository to run independently, additional dependency extraction and stubs are required.

## Create GitHub Repo And Push

```bash
cd /Users/shenqidelaofeng/Documents/YGO_build_x/replay-mode-player-component
git init
git add .
git commit -m "init: extract ReplayModePlayer component"
# replace with your github repo
git remote add origin git@github.com:<your-name>/replay-mode-player-component.git
git branch -M main
git push -u origin main
```
